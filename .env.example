# Web / 웹
# The Vercel demo can run without a backend because apps/web includes mock handlers.
# Vercel 데모는 apps/web의 mock handler만으로도 동작합니다.
MARKET_IMAGE_REMOTE_HOSTS=

# API / API
API_PORT=4000
DEMO_DATABASE_URL=postgresql://market_ops:market_ops@localhost:5432/market_ops_demo
